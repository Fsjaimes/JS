let nombre = "hola mundo";  
nombre = "Frank Jaimes";    //Todas las variables que declaramos con "let" pueden cambiar su valor en el futuro
const numero = 5;
numero = 4;  //Si la variable esta declarada como "const" saldrá error al ejecutar, el error sale en el navegador

console.log(nombre);